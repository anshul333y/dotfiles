#!/bin/bash

sudo apt update

printf "\n%${COLUMNS}s\n\n" | tr ' ' '-'

sudo apt upgrade

printf "\n%${COLUMNS}s\n\n" | tr ' ' '-'

sudo apt autoremove

printf "\n%${COLUMNS}s\n\n" | tr ' ' '-'

sudo snap refresh

printf "\n%${COLUMNS}s\n\n" | tr ' ' '-'

flatpak update
